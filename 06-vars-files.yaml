- name: variablesfrom files
  hosts: localhost
  vars_files:
  - vars.yaml
  tasks:
  - name: print override information
    ansible.builtin.debug:
      msg: "Hello., I am learning {{ Course }} from trainer {{ Trainer }}, duration is {{ Duration }}."    