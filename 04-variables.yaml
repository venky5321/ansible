- name: variables
  hosts: localhost
  vars:  #variable declaration
   course: "Terraform"
   trainer: "venky"
   Duration: "15hrs"
  tasks:
    - name: print information
      ansible.builtin.debug:
        msg: "Hello., I am learning {{ course }} from trainer {{ trainer }}, duration is {{ Duration }}."